bin_PROGRAMS = rcon
rcon_SOURCES = rcon.c command.c logfile.c access.c

# set the include path found by configure
INCLUDES= $(all_includes)

# the library search path.
rcon_LDFLAGS = $(all_libraries) 
noinst_HEADERS = command.h logfile.h access.h
